"""
Services package.

Currently empty because we haven't added any additional business logic.
In a larger project, this could contain things like:
    - Email notification service
    - Data export service (CSV, JSON)
    - Report generation
For this project, all core logic lives in `core/`.
"""